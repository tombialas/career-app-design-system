import AppShellDocContent from "./AppShellDocContent";

export const metadata = {
  title: "AppShell · Career Design System",
  description: "AppShell: page-level wrapper with optional sidebar, boxed vs canvas variants",
};

export default function AppShellDocPage() {
  return <AppShellDocContent />;
}

