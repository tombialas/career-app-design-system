import BadgeDocContent from "./BadgeDocContent";

export const metadata = {
  title: "Badge · Career Design System",
  description: "Badge: tiny indicator (PRO, counts), variants default/primary/destructive",
};

export default function BadgeDocPage() {
  return <BadgeDocContent />;
}

