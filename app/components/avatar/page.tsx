import AvatarDocContent from "./AvatarDocContent";

export const metadata = {
  title: "Avatar · Career Design System",
  description: "Avatar (Radix): image + fallback, rounded-full, B2C friendly",
};

export default function AvatarDocPage() {
  return <AvatarDocContent />;
}

